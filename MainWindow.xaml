<Window x:Class="SimpleToneGenerator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dauerton Generator" Height="220" Width="420">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Start / Stop + Amplitude -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Grid.Row="0" Margin="0,0,0,10">
            <Button Name="BtnStartStop" Width="100" Click="BtnStartStop_Click">Start</Button>
            <TextBlock Margin="10,0,0,0" VerticalAlignment="Center">Amplitude:</TextBlock>
            <Slider Name="AmpSlider" Minimum="0.0" Maximum="1.0" Value="0.25" Width="150" Margin="6,0,0,0" ValueChanged="AmpSlider_ValueChanged"/>
        </StackPanel>

        <!-- Frequenz Slider + Textbox -->
        <StackPanel Grid.Row="1" Margin="0,0,0,8">
            <TextBlock Text="Frequenz (Hz)" />
            <StackPanel Orientation="Horizontal">
                <Slider Name="FreqSlider"
                        Minimum="20" Maximum="20000" Value="440"
                        ValueChanged="FreqSlider_ValueChanged"
                        Width="250" />
                <TextBox Name="FreqTextBox"
                         Width="80"
                         Margin="10,0,0,0"
                         VerticalContentAlignment="Center"
                         Text="440"
                         KeyDown="FreqTextBox_KeyDown"
                         LostFocus="FreqTextBox_LostFocus"/>
            </StackPanel>
        </StackPanel>

        <!-- Aktuelle Frequenz Anzeige -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Aktuelle Frequenz: " FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBlock Name="FreqLabel" Text="440 Hz" VerticalAlignment="Center" Margin="6,0,0,0"/>
        </StackPanel>
    </Grid>
</Window>